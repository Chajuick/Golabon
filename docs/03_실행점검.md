# 골라본 실행 점검 결과

> 4단계: 빌드·구동·라우팅·SEO·고지·반응형 점검 + 남은 개선점(백로그)
> 점검일: 2026-06-03

---

## ✅ 검증 완료 항목

| 항목 | 결과 |
|---|---|
| 프로덕션 빌드 (`npm run build`) | ✅ 에러 0, 경고 0 |
| 정적 생성(SSG) | ✅ 30개 라우트 전부 정적 HTML 생성 |
| 상세 페이지 | ✅ 제품·장소 15건 각각 `/p/<id>/`로 독립 생성 |
| 카테고리 | ✅ 7개 카테고리 + 전체 목록 |
| 개발 서버 구동 | ✅ 전 라우트 HTTP 200, 콘솔 에러 없음 |
| **SEO — 본문 HTML 포함** | ✅ 리뷰 텍스트가 정적 HTML에 그대로 렌더(검색엔진이 읽음) |
| **SEO — 페이지별 메타** | ✅ `<title>`/`description`(=한줄판정) 페이지별 생성 |
| **SEO — 구조화 데이터** | ✅ 상세마다 `Review` JSON-LD 삽입 |
| 제휴 고지 | ✅ 홈·상세·푸터·전용 고지페이지에 노출 |
| 제휴 링크 처리 | ✅ `rel="sponsored nofollow noopener"` 자동 부여 |
| 반응형 | ✅ viewport 메타 + 원본 반응형 CSS 이식(모바일 그리드·드로어·하단 CTA) |
| 콘텐츠 입력 워크플로 | ✅ 데이터에 `photos`/`url` 주입 → HTML에 실제 `<img>`/`<a>` 생성 확인 |

---

## ⚠ 알려진 제약 / 다음 개선점 (백로그)

### 우선순위 높음 (출시 전)
- [ ] **배포 도메인 설정**: `app/layout.jsx`의 `metadataBase`를 실제 도메인으로 교체
- [ ] **sitemap.xml / robots.txt** 추가 (검색 색인 가속) — Next `app/sitemap.js`로 자동 생성 가능
- [ ] **실제 제휴 링크·트래킹 코드** 연결 (현재 빈 `url`은 안내 alert)
- [ ] **OG 이미지**: 공유 시 썸네일(현재 텍스트 메타만)
- [ ] 법·약관 체크리스트 이행 (→ `01_기획설계서.md` 8장)

### 우선순위 중간
- [ ] **검색 기능 고도화**: 현재 `?q=` 클라이언트 단순 필터(이름·소개·카테고리 매칭). 형태소·동의어는 미지원
- [ ] **찜(좋아요)**: localStorage 기반. 찜 목록 전용 페이지(`/liked`)는 미구현(헤더 하트는 카테고리로 연결)
- [ ] **이미지 최적화**: 정적 export라 `next/image` 최적화 off. 실제 사진은 업로드 전 리사이즈·webp 권장
- [ ] 카테고리 정렬·필터(추천순/최신순/가격순) 옵션

### 낮음 / 향후
- [ ] 조회수 등 동적 수치를 실제 분석 데이터와 연동
- [ ] 관리용 입력 폼(현재는 `lib/data.js` 직접 편집 — `docs/02` 가이드로 충분히 운영 가능)

---

## 메모

- 원본 23MB 단일 번들(`골라본 (오프라인).html`)은 보존돼 있으며, 편집은 이 Next.js 프로젝트에서 진행합니다.
- 원본에서 풀어둔 참고 소스는 `extracted/`에 있습니다.
- 디자인·문구·데이터 스키마는 원본 프로토타입을 그대로 계승했습니다(시각적 동일).
